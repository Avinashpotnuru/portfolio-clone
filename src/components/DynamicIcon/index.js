// import React from "react";

// import { contactDetails } from "@/src/Data";

// const DynamicIcon = ({ iconName }) => {
//   const Icon = { iconName };
//   //   const Icon = contactDetails[iconName]; // Get the icon component from the icons object

//   return (
//     <div>
//       <Icon size={50} /> {/* Render the icon */}
//     </div>
//   );
// };

// export default DynamicIcon;
